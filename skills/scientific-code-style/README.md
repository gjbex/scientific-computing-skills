`scientific-code-style` is a reusable skill for scientific-computing
and HPC repositories.

It is intended to hold cross-project conventions that should not live in a
single repository's `AGENTS.md`.
